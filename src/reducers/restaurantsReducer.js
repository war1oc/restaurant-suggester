const initialState = {
  restaurants: [],
  isLoading  : false
}

/**
 * Restaurants reducer
 * 
 * @param state
 * @param action
 * @returns {Object}
 */
export default function restaurantsReducer(state = initialState, action) {
  switch (action.type) {

    default: 
      break
  }

  return state
}